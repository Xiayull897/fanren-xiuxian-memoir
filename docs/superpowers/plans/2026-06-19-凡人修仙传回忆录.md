# 凡人修仙传 · 角色回忆录 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个静态角色回忆录网站首页（储物袋交互 + 角色法宝选择器）和角色详情页（RPG属性面板 + 境界时间线 + 关系 + 台词），包含韩立完整数据。

**Architecture:** 双页面静态站（index.html / character.html），通过 URL 参数传递角色 ID。JS 通过 fetch 加载 `data/characters/{id}/data.json` 和 `story.md`，动态渲染 DOM。CSS 自定义属性驱动暗色仙侠主题，角色专属色通过 JS 动态注入。

**Tech Stack:** HTML5 + CSS3（自定义属性、Grid、Flexbox、动画）+ Vanilla JS（Fetch API、DOM API）

---

## 文件结构总览

```
凡人回忆录/
├── index.html
├── character.html
├── css/
│   ├── style.css
│   ├── storage-bag.css
│   ├── character-card.css
│   └── timeline.css
├── js/
│   ├── data-loader.js
│   ├── cosmic-scale.js
│   ├── storage-bag.js
│   ├── character-page.js
│   └── timeline.js
├── data/
│   └── characters/
│       ├── index.json
│       ├── _template/
│       │   ├── data.json
│       │   └── story.md
│       └── hanli/
│           ├── data.json
│           └── story.md
├── assets/
│   └── icons/
├── docs/
│   └── superpowers/
│       ├── specs/2026-06-19-凡人修仙传回忆录-design.md
│       └── plans/2026-06-19-凡人修仙传回忆录.md
├── .gitignore
└── README.md
```

---

### Task 1: 项目骨架 + CSS 全局样式

**Files:**
- Create: `index.html`
- Create: `character.html`
- Create: `css/style.css`

- [ ] **Step 1: 创建 .gitignore**

写入 `D:\桌面凡人回忆录\.gitignore`：

```
.DS_Store
Thumbs.db
*.log
```

- [ ] **Step 2: 创建 index.html 骨架**

写入 `D:\桌面凡人回忆录\index.html`：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>凡人修仙传 · 角色回忆录</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@400;600;700;900&family=Noto+Sans+SC:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/style.css">
  <link rel="stylesheet" href="css/storage-bag.css">
</head>
<body>
  <div class="cosmic-bg"></div>
  <header class="site-header">
    <h1 class="site-title">凡人修仙传</h1>
    <p class="site-subtitle">角色回忆录</p>
  </header>
  <main id="app"></main>
  <script src="js/data-loader.js"></script>
  <script src="js/cosmic-scale.js"></script>
  <script src="js/storage-bag.js"></script>
</body>
</html>
```

- [ ] **Step 3: 创建 character.html 骨架**

写入 `D:\桌面凡人回忆录\character.html`：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>角色详情 — 凡人修仙传 · 角色回忆录</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@400;600;700;900&family=Noto+Sans+SC:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/style.css">
  <link rel="stylesheet" href="css/character-card.css">
  <link rel="stylesheet" href="css/timeline.css">
</head>
<body>
  <div class="cosmic-bg"></div>
  <nav class="breadcrumb">
    <a href="index.html" class="breadcrumb-link">← 返回首页</a>
    <span class="breadcrumb-sep">›</span>
    <span id="breadcrumb-name" class="breadcrumb-current">角色</span>
  </nav>
  <main id="character-app"></main>
  <script src="js/data-loader.js"></script>
  <script src="js/cosmic-scale.js"></script>
  <script src="js/timeline.js"></script>
  <script src="js/character-page.js"></script>
</body>
</html>
```

- [ ] **Step 4: 创建全局 CSS**

写入 `D:\桌面凡人回忆录\css\style.css`：

```css
/* === CSS 自定义属性 === */
:root {
  --bg-deep: #06060e;
  --bg-primary: #0d0d1a;
  --bg-card: #151528;
  --bg-card-hover: #1e1e38;
  --text-primary: #e6dfc8;
  --text-secondary: #9a9178;
  --text-dim: #5a5548;
  --gold: #c9a84c;
  --gold-light: #e8d48b;
  --gold-dark: #7a5f1e;
  --gold-glow: rgba(201, 168, 76, 0.3);
  --border-subtle: #1e1e30;
  --border-gold: #3d3010;
  --radius-sm: 6px;
  --radius-md: 12px;
  --radius-lg: 20px;
  --font-display: 'Noto Serif SC', serif;
  --font-body: 'Noto Sans SC', sans-serif;
  --shadow-gold: 0 0 20px rgba(201, 168, 76, 0.15);
}

/* === Reset === */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html { font-size: 16px; }

body {
  font-family: var(--font-body);
  background: var(--bg-deep);
  color: var(--text-primary);
  min-height: 100vh;
  overflow-x: hidden;
  line-height: 1.6;
}

/* === 宇宙背景 === */
.cosmic-bg {
  position: fixed;
  inset: 0;
  z-index: 0;
  background:
    radial-gradient(ellipse at 20% 20%, rgba(30, 20, 60, 0.4) 0%, transparent 60%),
    radial-gradient(ellipse at 80% 60%, rgba(10, 20, 50, 0.3) 0%, transparent 60%),
    radial-gradient(ellipse at 50% 80%, rgba(40, 20, 30, 0.2) 0%, transparent 50%),
    var(--bg-deep);
}

.breadcrumb,
#app,
#character-app,
.site-header,
#character-nav {
  position: relative;
  z-index: 1;
}

/* === 标题 === */
.site-header {
  text-align: center;
  padding: 60px 20px 40px;
}
.site-title {
  font-family: var(--font-display);
  font-size: 3rem;
  font-weight: 900;
  background: linear-gradient(180deg, var(--gold-light) 0%, var(--gold) 50%, var(--gold-dark) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  letter-spacing: 0.15em;
}
.site-subtitle {
  font-family: var(--font-display);
  font-size: 1.1rem;
  color: var(--text-secondary);
  margin-top: 8px;
  letter-spacing: 0.3em;
}

/* === 面包屑 === */
.breadcrumb {
  padding: 20px 30px;
  font-size: 0.9rem;
  color: var(--text-secondary);
}
.breadcrumb-link {
  color: var(--gold);
  text-decoration: none;
  transition: color 0.2s;
}
.breadcrumb-link:hover { color: var(--gold-light); }
.breadcrumb-sep { margin: 0 8px; color: var(--text-dim); }
.breadcrumb-current { color: var(--text-primary); }

/* === 通用工具类 === */
.sr-only {
  position: absolute; width: 1px; height: 1px;
  overflow: hidden; clip: rect(0,0,0,0); white-space: nowrap; border: 0;
}
```

- [ ] **Step 5: 验证 — 打开页面**

在浏览器中打开 `D:\桌面凡人回忆录\index.html`：
- Expected: 深色背景上有渐变色"凡人修仙传"标题和"角色回忆录"副标题，无报错

- [ ] **Step 6: 提交**

```bash
git add .gitignore index.html character.html css/style.css
git commit -m "feat: project scaffold with global CSS and page skeletons"
```

---

### Task 2: 角色数据 — 模板 + 韩立

**Files:**
- Create: `data/characters/_template/data.json`
- Create: `data/characters/_template/story.md`
- Create: `data/characters/index.json`
- Create: `data/characters/hanli/data.json`
- Create: `data/characters/hanli/story.md`

- [ ] **Step 1: 创建角色数据模板**

写入 `D:\桌面凡人回忆录\data\characters\_template\data.json`：

```json
{
  "id": "",
  "name": "",
  "alias": [],
  "color": "#c9a84c",
  "realm": { "name": "", "level": 0 },
  "realmHistory": [
    {
      "realm": "",
      "chapter": "",
      "episode": "",
      "event": "",
      "cosmicScale": ""
    }
  ],
  "fortunes": [
    {
      "name": "",
      "type": "机缘",
      "description": "",
      "realm": "",
      "significance": ""
    }
  ],
  "events": [
    {
      "name": "",
      "type": "事件",
      "description": "",
      "realm": "",
      "significance": ""
    }
  ],
  "voiceActor": { "cn": "", "jp": "" },
  "affiliation": "",
  "spiritualRoot": "",
  "treasures": [
    { "name": "", "rarity": "" }
  ],
  "relationships": [
    { "name": "", "relation": "", "note": "" }
  ],
  "quotes": [
    { "text": "", "context": "" }
  ]
}
```

写入 `D:\桌面凡人回忆录\data\characters\_template\story.md`：

```md
# 角色名

## 生平概述
（待补充）

## 性格特点
（待补充）

## 经典台词
- "..."（出处）
```

- [ ] **Step 2: 创建角色索引**

写入 `D:\桌面凡人回忆录\data\characters\index.json`：

```json
[
  {
    "id": "hanli",
    "name": "韩立",
    "alias": ["韩跑跑", "韩老魔"],
    "color": "#2d8a56",
    "realm": "大乘",
    "affiliation": "黄枫谷",
    "summary": "出身平凡的农家少年，凭借掌天瓶踏上修仙之路，一步步走向巅峰。"
  }
]
```

- [ ] **Step 3: 创建韩立 data.json**

写入 `D:\桌面凡人回忆录\data\characters\hanli\data.json`：

```json
{
  "id": "hanli",
  "name": "韩立",
  "alias": ["韩跑跑", "韩老魔"],
  "color": "#2d8a56",
  "realm": { "name": "大乘", "level": 10 },
  "realmHistory": [
    {
      "realm": "凡人",
      "chapter": "第1章",
      "episode": "第1集",
      "event": "出身七玄门，以凡人之躯被墨大夫收为弟子，经历地壳般的基础锤炼",
      "cosmicScale": "地球三层（地壳）"
    },
    {
      "realm": "凡人·地幔",
      "chapter": "第10章",
      "episode": "第3集",
      "event": "发现墨大夫真面目，被迫服用抽髓丸，踏上逆天改命之路，如地幔般在巨大压力中蜕变",
      "cosmicScale": "地球三层（地幔）"
    },
    {
      "realm": "凡人·地核",
      "chapter": "第20章",
      "episode": "第5集",
      "event": "获得神秘小瓶（掌天瓶），命运自此彻底改变，如地核般即将喷薄而出",
      "cosmicScale": "地球三层（地核）"
    },
    {
      "realm": "练气",
      "chapter": "第30章",
      "episode": "第8集",
      "event": "加入黄枫谷，正式踏入修仙之门，如同一颗初生的行星开始运转",
      "cosmicScale": "行星"
    },
    {
      "realm": "筑基",
      "chapter": "第100章",
      "episode": "第25集",
      "event": "成功筑基，在修仙界初露锋芒，以黄枫谷为中心建立了自己的势力体系",
      "cosmicScale": "恒星系统"
    },
    {
      "realm": "结丹",
      "chapter": "第300章",
      "episode": "第50集",
      "event": "历经九死一生结成金丹，实力进入全新层次，如星系般统御一方",
      "cosmicScale": "星系"
    },
    {
      "realm": "元婴",
      "chapter": "第600章",
      "episode": "第80集",
      "event": "元婴大成，成为人界顶尖修士，影响力跨越多个星域如同本星系群",
      "cosmicScale": "本星系群"
    },
    {
      "realm": "大乘",
      "chapter": "后续章节",
      "episode": "待更新",
      "event": "踏入大乘境界，俯瞰苍生，准备迎接飞升之劫",
      "cosmicScale": "超星系团"
    }
  ],
  "fortunes": [
    {
      "name": "获得掌天瓶",
      "type": "机缘",
      "description": "在七玄门后山意外获得神秘绿色小瓶，可催熟灵药、加速修炼，是韩立一生最大的机缘。",
      "realm": "凡人",
      "significance": "命运转折点"
    },
    {
      "name": "大衍诀",
      "type": "机缘",
      "description": "获得大衍神君的传承功法，可修炼强大神识，为后期纵横人界奠定基础。",
      "realm": "结丹",
      "significance": "核心功法"
    },
    {
      "name": "青元剑诀",
      "type": "机缘",
      "description": "获得青元子传承的剑诀，剑道修为大进。",
      "realm": "筑基",
      "significance": "剑道根基"
    }
  ],
  "events": [
    {
      "name": "七玄门入门",
      "type": "事件",
      "description": "被墨大夫收为弟子，在七玄门开始习武修身。",
      "realm": "凡人",
      "significance": "故事起点"
    },
    {
      "name": "智斗墨大夫",
      "type": "事件",
      "description": "识破墨大夫夺舍阴谋，反杀墨大夫，首次展现韩立的隐忍与智慧。",
      "realm": "凡人",
      "significance": "首次逆转"
    },
    {
      "name": "乱星海冒险",
      "type": "事件",
      "description": "进入乱星海，获得大量修炼资源，实力飞速提升。",
      "realm": "结丹",
      "significance": "重要转折"
    },
    {
      "name": "抗击魔界入侵",
      "type": "事件",
      "description": "人界遭遇魔界入侵，韩立挺身而出，成为人界最强战力之一。",
      "realm": "元婴",
      "significance": "英雄时刻"
    }
  ],
  "voiceActor": { "cn": "待补充", "jp": "待补充" },
  "affiliation": "黄枫谷",
  "spiritualRoot": "四灵根（伪灵根）",
  "treasures": [
    { "name": "掌天瓶", "rarity": "先天至宝" },
    { "name": "青竹蜂云剑", "rarity": "本命法宝" },
    { "name": "虚天鼎", "rarity": "通天灵宝" },
    { "name": "风雷翅", "rarity": "飞行法宝" }
  ],
  "relationships": [
    { "name": "南宫婉", "relation": "道侣", "note": "掩月宗长老，与韩立历经生死，终成眷属" },
    { "name": "紫灵", "relation": "红颜知己", "note": "乱星海第一美女，与韩立有不解之缘" },
    { "name": "元瑶", "relation": "旧识", "note": "曾与韩立同行一段修仙之路" },
    { "name": "大衍神君", "relation": "师父（传承）", "note": "留下大衍诀传承，对韩立影响深远" },
    { "name": "墨大夫", "relation": "仇敌/引路人", "note": "七玄门时期欲夺舍韩立，反被其所杀" }
  ],
  "quotes": [
    { "text": "我韩立行事，只求问心无愧。", "context": "面对质疑时的回答" },
    { "text": "留得青山在，不怕没柴烧。", "context": "韩立的人生信条，屡次以此自保" },
    { "text": "修仙之路，本就是逆天而行。", "context": "鼓励后辈时所说" }
  ]
}
```

- [ ] **Step 4: 创建韩立 story.md**

写入 `D:\桌面凡人回忆录\data\characters\hanli\story.md`：

```md
# 韩立

## 生平概述

韩立，出身于一个普通的农家，因机缘巧合被七玄门墨大夫收为弟子。在七玄门中，他意外获得了一只神秘的绿色小瓶——掌天瓶，这个小瓶能催熟灵药，成为他修仙路上最大的依仗。

他天资平平，仅为四灵根（伪灵根），在修仙界原本毫无前途可言。然而凭借掌天瓶的逆天功效，以及自身谨慎隐忍、步步为营的性格，他从最底层一步步向上攀登，先后经历了练气、筑基、结丹、元婴，最终成就大乘。

韩立的一生如同一颗种子从地球深处破土而出，成长为一颗行星，再扩展为恒星系统，最终化为整个星系群——他的境界提升恰似宇宙的诞生与扩张。

## 性格特点

- **谨慎隐忍**：从不冒险，永远给自己留后路，"韩跑跑"之名由此而来
- **重情重义**：对朋友有恩必报，对敌人斩草除根
- **坚韧不拔**：伪灵根的先天劣势从未阻挡他的脚步
- **低调务实**：不求虚名，只求长生大道

## 经典台词

- "我韩立行事，只求问心无愧。"
- "留得青山在，不怕没柴烧。"
- "修仙之路，本就是逆天而行。"
- "若连自己的命运都无法掌握，还修什么仙？"
```

- [ ] **Step 5: 创建资产目录**

```bash
mkdir -p "D:\桌面凡人回忆录\assets\icons"
```

- [ ] **Step 6: 提交**

```bash
git add data/
git commit -m "feat: add character data model, Han Li data, and index"
```

---

### Task 3: 数据加载 + 宇宙尺度映射

**Files:**
- Create: `js/data-loader.js`
- Create: `js/cosmic-scale.js`

- [ ] **Step 1: 创建宇宙尺度配置模块**

写入 `D:\桌面凡人回忆录\js\cosmic-scale.js`：

```javascript
/**
 * 境界 → 宇宙尺度映射配置
 * 用于视觉渲染：背景渐变、节点颜色、指示器
 */
const COSMIC_SCALES = {
  '地球三层（地壳）': {
    level: 0,
    label: '地壳',
    gradient: 'linear-gradient(135deg, #2d1a0a 0%, #5a3020 50%, #8b4513 100%)',
    accent: '#c4621e',
    icon: '🌋',
  },
  '地球三层（地幔）': {
    level: 0.5,
    label: '地幔',
    gradient: 'linear-gradient(135deg, #5a3020 0%, #b8450e 50%, #ff6a1a 100%)',
    accent: '#e0551e',
    icon: '🔥',
  },
  '地球三层（地核）': {
    level: 0.8,
    label: '地核',
    gradient: 'linear-gradient(135deg, #b8450e 0%, #ff5500 50%, #ffcc00 100%)',
    accent: '#ff6600',
    icon: '💎',
  },
  '行星': {
    level: 1,
    label: '练气 · 行星',
    gradient: 'linear-gradient(135deg, #0a1628 0%, #1a3a5c 50%, #2a6090 100%)',
    accent: '#4a90d9',
    icon: '🪐',
  },
  '恒星系统': {
    level: 2,
    label: '筑基 · 恒星系统',
    gradient: 'linear-gradient(135deg, #0a0a1a 0%, #1a1040 50%, #f0a030 100%)',
    accent: '#f0a030',
    icon: '☀️',
  },
  '星系': {
    level: 3,
    label: '结丹 · 星系',
    gradient: 'linear-gradient(135deg, #050520 0%, #200840 50%, #9060d0 100%)',
    accent: '#9060d0',
    icon: '🌌',
  },
  '本星系群': {
    level: 4,
    label: '元婴 · 本星系群',
    gradient: 'linear-gradient(135deg, #020210 0%, #100830 50%, #e060c0 100%)',
    accent: '#c060a0',
    icon: '🫧',
  },
  '超星系团': {
    level: 5,
    label: '大乘 · 超星系团',
    gradient: 'linear-gradient(135deg, #000010 0%, #050430 50%, #f0e0ff 100%)',
    accent: '#d0b0f0',
    icon: '✨',
  },
};

/**
 * 根据 cosmicScale 字符串获取配置
 * @param {string} scaleKey - 如 '恒星系统'
 * @returns {object} 对应的尺度配置，找不到返回默认值
 */
function getCosmicScale(scaleKey) {
  return COSMIC_SCALES[scaleKey] || {
    level: 0,
    label: scaleKey || '未知',
    gradient: 'linear-gradient(135deg, #0a0a0a, #1a1a1a)',
    accent: '#888',
    icon: '❓',
  };
}

/**
 * 获取当前境界对应的最高宇宙尺度
 * @param {string} realmName - 境界名，如 '大乘'
 * @returns {object} 最高匹配的尺度配置
 */
function getScaleByRealm(realmName) {
  const map = {
    '凡人': '地球三层（地壳）',
    '练气': '行星',
    '筑基': '恒星系统',
    '结丹': '星系',
    '元婴': '本星系群',
    '化神': '超星系团',
    '炼虚': '超星系团',
    '合体': '超星系团',
    '大乘': '超星系团',
    '渡劫': '超星系团',
    '真仙': '超星系团',
  };
  return getCosmicScale(map[realmName] || '');
}
```

- [ ] **Step 2: 创建数据加载模块**

写入 `D:\桌面凡人回忆录\js\data-loader.js`：

```javascript
/**
 * 数据加载模块
 * 负责从 data/ 目录加载 JSON 和 Markdown 文件
 */

const DATA_BASE = 'data/characters/';

/**
 * 加载角色列表索引
 * @returns {Promise<Array>} 角色摘要数组
 */
async function loadCharacterIndex() {
  const res = await fetch(DATA_BASE + 'index.json');
  if (!res.ok) {
    throw new Error(`加载角色列表失败: HTTP ${res.status}`);
  }
  return res.json();
}

/**
 * 加载单个角色的结构化数据
 * @param {string} id - 角色 ID，如 'hanli'
 * @returns {Promise<Object>} 角色 data.json 内容
 */
async function loadCharacterData(id) {
  const res = await fetch(`${DATA_BASE}${id}/data.json`);
  if (!res.ok) {
    throw new Error(`加载角色数据失败 (${id}): HTTP ${res.status}`);
  }
  return res.json();
}

/**
 * 加载角色传记 Markdown
 * @param {string} id - 角色 ID
 * @returns {Promise<string>} Markdown 原文
 */
async function loadCharacterStory(id) {
  const res = await fetch(`${DATA_BASE}${id}/story.md`);
  if (!res.ok) {
    return ''; // story.md 可选
  }
  return res.text();
}

/**
 * 简易 Markdown → HTML 转换（仅处理本项目的有限格式）
 * @param {string} md - Markdown 源码
 * @returns {string} HTML 字符串
 */
function simpleMarkdownToHtml(md) {
  if (!md) return '';

  let html = md
    // 标题
    .replace(/^### (.+)$/gm, '<h3 class="story-h3">$1</h3>')
    .replace(/^## (.+)$/gm, '<h2 class="story-h2">$1</h2>')
    .replace(/^# (.+)$/gm, '<h1 class="story-h1">$1</h1>')
    // 列表
    .replace(/^- (.+)$/gm, '<li>$1</li>')
    .replace(/(<li>.*<\/li>\n?)+/g, '<ul>$&</ul>')
    // 粗体
    .replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
    // 段落：连续的非空行
    .replace(/\n\n/g, '</p><p>')
    .replace(/^(?!<[hul/])/gm, '');

  html = '<p>' + html + '</p>';
  // 清理空标签
  html = html.replace(/<(h[123]|ul|p)>\s*<\/\1>/g, '');
  html = html.replace(/<p>\s*<p>/g, '<p>');
  html = html.replace(/<\/p>\s*<\/p>/g, '</p>');

  return html;
}
```

- [ ] **Step 3: 验证数据加载 — 在浏览器控制台测试**

在浏览器中打开 `index.html`，打开控制台（F12），执行：

```javascript
const idx = await loadCharacterIndex();
console.log('角色列表:', idx);
const data = await loadCharacterData('hanli');
console.log('韩立数据:', data);
const story = await loadCharacterStory('hanli');
console.log('韩立传记:', story);
```

Expected: 三个调用均成功返回数据，无 404 错误。

- [ ] **Step 4: 提交**

```bash
git add js/data-loader.js js/cosmic-scale.js
git commit -m "feat: add data loader and cosmic scale mapping modules"
```

---

### Task 4: 储物袋 CSS + 首页结构

**Files:**
- Create: `css/storage-bag.css`
- Modify: `index.html` — 在 `<main id="app">` 内添加储物袋 HTML

- [ ] **Step 1: 写入储物袋 CSS**

写入 `D:\桌面凡人回忆录\css\storage-bag.css`：

```css
/* === 储物袋页面布局 === */
.storage-page {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 70vh;
  padding: 20px;
}

/* === 储物袋容器 === */
.bag-container {
  position: relative;
  width: 160px;
  height: 200px;
  cursor: pointer;
  transition: transform 0.3s ease;
  z-index: 10;
}
.bag-container:hover { transform: scale(1.05); }
.bag-container:hover .bag-body { filter: brightness(1.1); }

/* === 储物袋 CSS 绘制 === */
.bag-body {
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 120px;
  height: 140px;
  background: linear-gradient(180deg, #3d2817 0%, #5a3a20 30%, #4a2d18 60%, #2d1a0d 100%);
  border-radius: 20px 20px 40px 40px / 30px 30px 50px 50px;
  box-shadow:
    inset 0 2px 8px rgba(255, 220, 150, 0.1),
    0 4px 20px rgba(0, 0, 0, 0.5);
  transition: all 0.5s ease;
}
/* 袋口 */
.bag-body::before {
  content: '';
  position: absolute;
  top: -2px;
  left: 10%;
  width: 80%;
  height: 20px;
  background: linear-gradient(180deg, #5a3820, #3d2010);
  border-radius: 6px;
  box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.5);
}
/* 袋口束绳 */
.bag-rope {
  position: absolute;
  top: 16px;
  left: 50%;
  transform: translateX(-50%);
  width: 100px;
  height: 4px;
  background: var(--gold);
  border-radius: 2px;
  box-shadow: 0 0 8px var(--gold-glow);
}
/* 袋子褶皱纹理 */
.bag-body::after {
  content: '';
  position: absolute;
  top: 25%;
  left: 30%;
  width: 40%;
  height: 50%;
  background: linear-gradient(180deg, transparent 0%, rgba(0, 0, 0, 0.2) 50%, transparent 100%);
  border-radius: 50%;
}

/* === 储物袋浮动动画 === */
@keyframes bagFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-12px); }
}
.bag-container.floating {
  animation: bagFloat 3s ease-in-out infinite;
}

/* === 储物袋打开状态 === */
.bag-container.opened .bag-body {
  border-radius: 20px 20px 10px 10px / 30px 30px 10px 10px;
  height: 100px;
  background: linear-gradient(180deg, #3d2817 0%, #5a3a20 50%, #4a2d18 100%);
}
.bag-container.opened .bag-rope {
  top: 10px;
  opacity: 0.5;
}

/* === 提示文字 === */
.bag-hint {
  margin-top: 20px;
  color: var(--text-secondary);
  font-family: var(--font-display);
  font-size: 0.95rem;
  letter-spacing: 0.1em;
  transition: opacity 0.3s;
  text-align: center;
}

/* === 法宝展示区域 === */
.treasures-area {
  position: relative;
  width: 100%;
  max-width: 900px;
  min-height: 400px;
  margin-top: -40px;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  gap: 30px;
  padding: 40px 20px;
  z-index: 5;
}

/* === 单个法宝卡片 === */
.treasure-card {
  width: 100px;
  height: 130px;
  background: var(--bg-card);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-md);
  cursor: pointer;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  transition: all 0.3s ease;
  position: relative;
  opacity: 0;
  transform: scale(0) translateY(50px);
}

.treasure-card.visible {
  opacity: 1;
  transform: scale(1) translateY(0);
}

.treasure-card:hover {
  transform: translateY(-8px) scale(1.05);
  background: var(--bg-card-hover);
}

/* 法宝光晕：角色专属色 */
.treasure-card::before {
  content: '';
  position: absolute;
  inset: -2px;
  border-radius: inherit;
  background: var(--treasure-glow, var(--gold));
  opacity: 0;
  z-index: -1;
  transition: opacity 0.3s;
}
.treasure-card:hover::before {
  opacity: 0.6;
  filter: blur(12px);
}

.treasure-icon {
  font-size: 2rem;
}
.treasure-name {
  font-family: var(--font-display);
  font-size: 0.95rem;
  font-weight: 600;
  color: var(--text-primary);
}
.treasure-realm {
  font-size: 0.7rem;
  color: var(--text-secondary);
}

/* === 法宝出现动画（依次弹出） === */
@keyframes treasurePop {
  0% { opacity: 0; transform: scale(0) translateY(80px) rotate(-20deg); }
  60% { opacity: 1; transform: scale(1.15) translateY(-10px) rotate(3deg); }
  100% { opacity: 1; transform: scale(1) translateY(0) rotate(0deg); }
}
.treasure-card.animated {
  animation: treasurePop 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
}

/* === 筛选栏 === */
.filter-bar {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  justify-content: center;
  margin-bottom: 20px;
  opacity: 0;
  transition: opacity 0.5s 0.3s;
}
.filter-bar.visible { opacity: 1; }
.filter-btn {
  background: var(--bg-card);
  color: var(--text-secondary);
  border: 1px solid var(--border-subtle);
  padding: 6px 16px;
  border-radius: 20px;
  font-family: var(--font-body);
  font-size: 0.85rem;
  cursor: pointer;
  transition: all 0.2s;
}
.filter-btn:hover, .filter-btn.active {
  color: var(--gold-light);
  border-color: var(--border-gold);
  background: var(--bg-card-hover);
}
```

- [ ] **Step 2: 在 index.html 中添加储物袋 HTML**

修改 `D:\桌面凡人回忆录\index.html`，将 `<main id="app"></main>` 替换为：

```html
<main id="app">
  <div class="storage-page">
    <div class="filter-bar" id="filter-bar">
      <button class="filter-btn active" data-filter="all">全部</button>
    </div>
    <div class="bag-container floating" id="bag">
      <div class="bag-body"></div>
      <div class="bag-rope"></div>
    </div>
    <p class="bag-hint" id="bag-hint">点击储物袋，查看角色法宝</p>
    <div class="treasures-area" id="treasures"></div>
  </div>
</main>
```

- [ ] **Step 3: 验证 — 打开 index.html**

Expected: 页面中央显示一个棕色储物袋（CSS 绘制），缓慢上下浮动，下方显示提示文字"点击储物袋，查看角色法宝"。

- [ ] **Step 4: 提交**

```bash
git add css/storage-bag.css index.html
git commit -m "feat: add storage bag CSS art and homepage layout"
```

---

### Task 5: 储物袋 JS 交互

**Files:**
- Create: `js/storage-bag.js`
- Modify: `index.html` — 添加角色切换导航（可选）

- [ ] **Step 1: 写入储物袋交互脚本**

写入 `D:\桌面凡人回忆录\js\storage-bag.js`：

```javascript
/**
 * 储物袋交互模块
 * 处理首页储物袋点击 → 法宝飞出的交互
 */

(function () {
  let isOpen = false;
  let characterList = [];

  const bag = document.getElementById('bag');
  const hint = document.getElementById('bag-hint');
  const treasuresArea = document.getElementById('treasures');
  const filterBar = document.getElementById('filter-bar');

  /**
   * 初始化：预加载角色列表
   */
  async function init() {
    try {
      characterList = await loadCharacterIndex();
      renderFilters(characterList);
    } catch (err) {
      console.error('储物袋初始化失败:', err);
      hint.textContent = '加载失败，请刷新页面重试';
    }
  }

  /**
   * 生成筛选按钮
   */
  function renderFilters(characters) {
    const realms = [...new Set(characters.map(c => c.realm))];
    realms.forEach(realm => {
      const btn = document.createElement('button');
      btn.className = 'filter-btn';
      btn.dataset.filter = realm;
      btn.textContent = realm;
      btn.addEventListener('click', () => filterTreasures(realm, btn));
      filterBar.appendChild(btn);
    });

    // 全部按钮事件
    filterBar.querySelector('[data-filter="all"]').addEventListener('click', function () {
      filterTreasures('all', this);
    });
  }

  /**
   * 筛选法宝
   */
  function filterTreasures(filter, activeBtn) {
    filterBar.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
    activeBtn.classList.add('active');

    const cards = treasuresArea.querySelectorAll('.treasure-card');
    cards.forEach(card => {
      if (filter === 'all' || card.dataset.realm === filter) {
        card.style.display = '';
      } else {
        card.style.display = 'none';
      }
    });
  }

  /**
   * 储物袋点击事件
   */
  bag.addEventListener('click', async function () {
    if (isOpen) {
      closeBag();
    } else {
      await openBag();
    }
  });

  /**
   * 打开储物袋：动画 + 生成法宝
   */
  async function openBag() {
    isOpen = true;
    bag.classList.add('opened');
    bag.classList.remove('floating');
    hint.textContent = '点击法宝查看角色详情';

    // 确保数据已加载
    if (characterList.length === 0) {
      try {
        characterList = await loadCharacterIndex();
        renderFilters(characterList);
      } catch (err) {
        hint.textContent = '加载失败，请重试';
        return;
      }
    }

    // 清空旧法宝
    treasuresArea.innerHTML = '';

    // 依次生成法宝卡片，带延迟动画
    characterList.forEach((char, index) => {
      const card = createTreasureCard(char, index);
      treasuresArea.appendChild(card);

      // 延迟触发弹出动画
      setTimeout(() => {
        card.classList.add('animated', 'visible');
      }, 100 + index * 120);
    });

    // 显示筛选栏
    filterBar.classList.add('visible');
  }

  /**
   * 关闭储物袋（后续实现：法宝缩回动画，暂简化为直接收起）
   */
  function closeBag() {
    isOpen = false;
    bag.classList.remove('opened');
    bag.classList.add('floating');
    hint.textContent = '点击储物袋，查看角色法宝';
    treasuresArea.innerHTML = '';
    filterBar.classList.remove('visible');
  }

  /**
   * 创建法宝卡片 DOM
   */
  function createTreasureCard(char, index) {
    const card = document.createElement('div');
    card.className = 'treasure-card';
    card.dataset.realm = char.realm;
    card.style.setProperty('--treasure-glow', char.color);
    card.style.animationDelay = `${index * 0.12}s`;

    const scale = getScaleByRealm(char.realm);

    card.innerHTML = `
      <span class="treasure-icon">${scale.icon}</span>
      <span class="treasure-name">${char.name}</span>
      <span class="treasure-realm">${char.realm}</span>
    `;

    // 点击法宝 → 跳转详情页
    card.addEventListener('click', () => {
      window.location.href = `character.html?id=${char.id}`;
    });

    return card;
  }

  // 启动
  init();
})();
```

- [ ] **Step 2: 验证 — 打开 index.html，点击储物袋**

Expected:
1. 页面加载，储物袋浮动
2. 点击储物袋 → 袋子打开（变形），韩立的法宝卡片弹出（绿色光晕，带"韩立"和"大乘"）
3. 筛选栏出现在顶部
4. 法宝 hover 时上浮 + 光晕显示
5. 点击法宝 → 跳转到 `character.html?id=hanli`（目前 404，下一步实现）

- [ ] **Step 3: 提交**

```bash
git add js/storage-bag.js
git commit -m "feat: implement storage bag open/close and treasure card interaction"
```

---

### Task 6: 角色详情页 — 属性面板

**Files:**
- Create: `css/character-card.css`
- Create: `js/character-page.js`
- Modify: `character.html` — 无需修改，JS 全动态渲染

- [ ] **Step 1: 写入属性面板 CSS**

写入 `D:\桌面凡人回忆录\css\character-card.css`：

```css
/* === 角色详情页布局 === */
.character-page {
  max-width: 860px;
  margin: 0 auto;
  padding: 0 20px 60px;
}

/* === 属性面板 === */
.stat-panel {
  background: var(--bg-card);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: var(--shadow-gold);
  margin-bottom: 40px;
}

.stat-panel-header {
  padding: 30px 36px 20px;
  position: relative;
  overflow: hidden;
}

/* 宇宙视野背景 */
.stat-panel-header::before {
  content: '';
  position: absolute;
  inset: 0;
  opacity: 0.15;
  background: var(--cosmic-gradient, var(--bg-card));
  z-index: 0;
}

.stat-panel-header > * {
  position: relative;
  z-index: 1;
}

.character-name {
  font-family: var(--font-display);
  font-size: 2.4rem;
  font-weight: 900;
  color: var(--char-color, var(--gold));
  text-shadow: 0 0 30px var(--char-glow, var(--gold-glow));
  margin-bottom: 4px;
}

.character-aliases {
  font-size: 0.9rem;
  color: var(--text-secondary);
  margin-bottom: 8px;
}
.character-aliases span {
  display: inline-block;
  background: rgba(255,255,255,0.05);
  padding: 2px 10px;
  border-radius: 12px;
  margin-right: 6px;
}

.character-title-line {
  display: flex;
  gap: 20px;
  flex-wrap: wrap;
  margin-top: 12px;
  font-size: 0.9rem;
  color: var(--text-secondary);
}
.character-title-line strong {
  color: var(--text-primary);
  margin-right: 4px;
}

/* === 宇宙视野指示器 === */
.cosmic-indicator {
  margin: 0 36px 20px;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 16px;
  background: rgba(0,0,0,0.3);
  border-radius: var(--radius-sm);
  border: 1px solid var(--border-subtle);
}
.cosmic-indicator-label {
  font-size: 0.8rem;
  color: var(--text-dim);
  white-space: nowrap;
}
.cosmic-indicator-scale {
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--char-color, var(--gold));
  font-family: var(--font-display);
}
.cosmic-indicator-icon {
  font-size: 1.8rem;
}

/* === 属性表格 === */
.stat-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 1px;
  background: var(--border-subtle);
  margin: 0 1px 1px;
}
.stat-item {
  background: var(--bg-primary);
  padding: 14px 18px;
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.stat-label {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-dim);
}
.stat-value {
  font-size: 1rem;
  font-weight: 500;
  color: var(--text-primary);
}

/* === 法宝/功法网格 === */
.treasure-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
  gap: 12px;
  padding: 20px 36px;
  border-top: 1px solid var(--border-subtle);
}
.treasure-grid-title {
  grid-column: 1 / -1;
  font-family: var(--font-display);
  font-size: 1rem;
  color: var(--gold);
  margin-bottom: 4px;
}
.treasure-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 14px;
  background: rgba(0,0,0,0.2);
  border-radius: var(--radius-sm);
  border: 1px solid var(--border-subtle);
}
.treasure-rarity {
  font-size: 0.65rem;
  padding: 2px 8px;
  border-radius: 10px;
  background: var(--gold-dark);
  color: var(--gold-light);
  white-space: nowrap;
}
.treasure-item-name {
  font-size: 0.9rem;
  color: var(--text-primary);
}
```

- [ ] **Step 2: 写入角色页面渲染脚本**

写入 `D:\桌面凡人回忆录\js\character-page.js`：

```javascript
/**
 * 角色详情页渲染模块
 * 读取 URL 参数 ?id=xxx，加载对应角色数据，渲染属性面板
 */

(function () {
  const app = document.getElementById('character-app');
  const breadcrumbName = document.getElementById('breadcrumb-name');

  async function init() {
    const id = new URLSearchParams(window.location.search).get('id');
    if (!id) {
      app.innerHTML = '<div class="character-page"><p style="text-align:center;color:var(--text-dim);padding:80px 20px;">未指定角色 — 请从首页选择一位角色</p></div>';
      return;
    }

    try {
      const [data, story] = await Promise.all([
        loadCharacterData(id),
        loadCharacterStory(id),
      ]);

      document.title = `${data.name} — 凡人修仙传 · 角色回忆录`;
      breadcrumbName.textContent = data.name;
      renderCharacterPage(data, story);
    } catch (err) {
      console.error('加载角色失败:', err);
      app.innerHTML = `<div class="character-page"><p style="text-align:center;color:#c44;padding:80px 20px;">加载失败：${err.message}</p></div>`;
    }
  }

  function renderCharacterPage(data, storyMd) {
    const scale = getScaleByRealm(data.realm.name);

    const aliasesHtml = data.alias.length
      ? `<div class="character-aliases">${data.alias.map(a => `<span>${a}</span>`).join('')}</div>`
      : '';

    const voiceHtml = data.voiceActor.cn
      ? `<span><strong>CV：</strong>${data.voiceActor.cn}</span>`
      : '';

    const affiliationHtml = data.affiliation
      ? `<span><strong>宗门：</strong>${data.affiliation}</span>`
      : '';

    const rootHtml = data.spiritualRoot
      ? `<span><strong>灵根：</strong>${data.spiritualRoot}</span>`
      : '';

    const treasuresHtml = data.treasures.length ? `
      <div class="treasure-grid">
        <div class="treasure-grid-title">⚔️ 法宝 / 功法</div>
        ${data.treasures.map(t => `
          <div class="treasure-item">
            <span class="treasure-rarity">${t.rarity}</span>
            <span class="treasure-item-name">${t.name}</span>
          </div>
        `).join('')}
      </div>
    ` : '';

    const storyHtml = storyMd
      ? `<section class="story-section"><div class="story-content">${simpleMarkdownToHtml(storyMd)}</div></section>`
      : '';

    app.innerHTML = `
      <div class="character-page">
        <div class="stat-panel">
          <div class="stat-panel-header" style="--cosmic-gradient: ${scale.gradient};">
            <h1 class="character-name" style="--char-color: ${data.color}; --char-glow: ${data.color}44;">${data.name}</h1>
            ${aliasesHtml}
            <div class="character-title-line">
              <span><strong>境界：</strong>${data.realm.name}</span>
              ${voiceHtml}
              ${affiliationHtml}
              ${rootHtml}
            </div>
          </div>

          <div class="cosmic-indicator">
            <span class="cosmic-indicator-icon">${scale.icon}</span>
            <span class="cosmic-indicator-label">宇宙视野</span>
            <span class="cosmic-indicator-scale" style="--char-color: ${data.color};">${scale.label}</span>
          </div>

          <div class="stat-grid">
            <div class="stat-item">
              <span class="stat-label">当前修为</span>
              <span class="stat-value">${data.realm.name}</span>
            </div>
            <div class="stat-item">
              <span class="stat-label">宗门势力</span>
              <span class="stat-value">${data.affiliation || '暂无'}</span>
            </div>
            <div class="stat-item">
              <span class="stat-label">灵根资质</span>
              <span class="stat-value">${data.spiritualRoot || '暂无'}</span>
            </div>
            <div class="stat-item">
              <span class="stat-label">配音演员</span>
              <span class="stat-value">${data.voiceActor.cn || '待补充'}</span>
            </div>
          </div>

          ${treasuresHtml}
        </div>

        ${storyHtml}

        <div id="timeline-container"></div>
        <div id="relationships-container"></div>
        <div id="quotes-container"></div>
      </div>
    `;

    // 触发后续组件渲染（使用自定义事件解耦）
    document.dispatchEvent(new CustomEvent('character-loaded', { detail: data }));
  }

  init();
})();
```

- [ ] **Step 3: 验证 — 在浏览器中打开 character.html?id=hanli**

Expected:
1. 显示韩立的属性面板，名字为青绿色 `#2d8a56`，含光晕
2. 显示别名"韩跑跑""韩老魔"
3. 宇宙视野指示器显示"大乘 · 超星系团"及对应 icon
4. 属性表格显示修为、宗门、灵根、CV
5. 法宝/功法以稀有度标签展示

- [ ] **Step 4: 添加传记区域样式**

在 `character-card.css` 末尾追加：

```css
/* === 传记区域 === */
.story-section {
  margin-bottom: 40px;
  padding: 28px 36px;
  background: var(--bg-card);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-lg);
}
.story-content { color: var(--text-secondary); line-height: 1.8; }
.story-h1 { font-family: var(--font-display); color: var(--gold); font-size: 1.5rem; margin-bottom: 16px; }
.story-h2 { font-family: var(--font-display); color: var(--gold-light); font-size: 1.2rem; margin: 20px 0 12px; padding-bottom: 8px; border-bottom: 1px solid var(--border-subtle); }
.story-h3 { font-family: var(--font-display); color: var(--text-primary); font-size: 1rem; margin: 16px 0 8px; }
.story-content ul { padding-left: 20px; margin: 8px 0; }
.story-content li { margin-bottom: 4px; }
.story-content strong { color: var(--text-primary); }
```

- [ ] **Step 5: 提交**

```bash
git add css/character-card.css js/character-page.js
git commit -m "feat: add character stat panel and page rendering"
```

---

### Task 7: 境界时间线

**Files:**
- Create: `css/timeline.css`
- Create: `js/timeline.js`

- [ ] **Step 1: 写入时间线 CSS**

写入 `D:\桌面凡人回忆录\css\timeline.css`：

```css
/* === 时间线容器 === */
.timeline-section {
  margin-bottom: 40px;
}
.timeline-title {
  font-family: var(--font-display);
  font-size: 1.3rem;
  color: var(--gold);
  margin-bottom: 24px;
  padding-left: 12px;
  border-left: 3px solid var(--gold);
}

.timeline {
  position: relative;
  padding-left: 40px;
}

/* 时间线中轴线 */
.timeline::before {
  content: '';
  position: absolute;
  left: 18px;
  top: 8px;
  bottom: 8px;
  width: 2px;
  background: linear-gradient(180deg,
    var(--border-gold) 0%,
    var(--gold) 50%,
    var(--border-gold) 100%
  );
}

/* === 时间线节点 === */
.timeline-node {
  position: relative;
  margin-bottom: 32px;
  padding: 16px 20px;
  background: var(--bg-card);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-md);
  transition: background 0.2s;
}
.timeline-node:hover {
  background: var(--bg-card-hover);
}

/* 节点圆点 */
.timeline-node::before {
  content: '';
  position: absolute;
  left: -30px;
  top: 22px;
  width: 14px;
  height: 14px;
  border-radius: 50%;
  background: var(--node-accent, var(--gold));
  box-shadow: 0 0 10px var(--node-glow, var(--gold-glow));
  border: 2px solid var(--bg-deep);
  z-index: 1;
}

/* 境界突破 — 大节点 */
.timeline-node.realm-breakthrough::before {
  width: 20px;
  height: 20px;
  left: -33px;
  top: 20px;
  box-shadow: 0 0 18px var(--node-glow, var(--gold-glow));
}

/* 境界突破节点背景 */
.timeline-node.realm-breakthrough {
  border-color: var(--node-accent, var(--border-gold));
  position: relative;
  overflow: hidden;
}
.timeline-node.realm-breakthrough::after {
  content: '';
  position: absolute;
  inset: 0;
  opacity: 0.06;
  background: var(--node-gradient, transparent);
  z-index: 0;
}
.timeline-node.realm-breakthrough > * {
  position: relative;
  z-index: 1;
}

/* === 节点内容 === */
.node-header {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 6px;
}
.node-realm {
  font-family: var(--font-display);
  font-size: 1.05rem;
  font-weight: 700;
  color: var(--node-accent, var(--gold));
}
.node-badge {
  font-size: 0.7rem;
  padding: 2px 10px;
  border-radius: 10px;
  background: var(--node-accent, var(--gold-dark));
  color: #fff;
  opacity: 0.8;
}
.node-description {
  font-size: 0.9rem;
  color: var(--text-secondary);
  line-height: 1.6;
}
.node-meta {
  margin-top: 8px;
  font-size: 0.75rem;
  color: var(--text-dim);
  display: flex;
  gap: 12px;
}

/* === 机缘/事件小节点 === */
.timeline-node.event-node {
  padding: 12px 16px;
  margin-bottom: 20px;
  border-left: 3px solid var(--node-accent, var(--gold));
}
.timeline-node.event-node::before {
  width: 10px;
  height: 10px;
  left: -28px;
  top: 18px;
  box-shadow: 0 0 6px var(--node-glow, var(--gold-glow));
}
.timeline-node.event-node.fortune {
  border-left-color: #c9a84c;
}
.timeline-node.event-node.fortune::before {
  background: var(--gold);
}
.event-tag {
  font-size: 0.7rem;
  padding: 2px 8px;
  border-radius: 8px;
  display: inline-block;
  margin-bottom: 4px;
}
.event-tag.fortune { background: rgba(201, 168, 76, 0.2); color: var(--gold-light); }
.event-tag.event { background: rgba(100, 160, 200, 0.2); color: #8ac; }
```

- [ ] **Step 2: 写入时间线渲染脚本**

写入 `D:\桌面凡人回忆录\js\timeline.js`：

```javascript
/**
 * 境界时间线渲染模块
 * 监听 character-loaded 事件，渲染时间线
 */

(function () {
  document.addEventListener('character-loaded', function (e) {
    renderTimeline(e.detail);
    renderRelationships(e.detail);
    renderQuotes(e.detail);
  });

  function renderTimeline(data) {
    const container = document.getElementById('timeline-container');
    if (!container) return;

    // 构建时间线条目：境界突破 + 机缘 + 事件
    const items = buildTimelineItems(data);

    container.innerHTML = `
      <section class="timeline-section">
        <h2 class="timeline-title">修仙之路</h2>
        <div class="timeline">
          ${items.map(item => renderTimelineNode(item, data.color)).join('')}
        </div>
      </section>
    `;
  }

  function buildTimelineItems(data) {
    const items = [];

    // 将 realmHistory、fortunes、events 合并，按境界排序
    const realmOrder = ['凡人', '练气', '筑基', '结丹', '元婴', '化神', '炼虚', '合体', '大乘', '渡劫'];

    data.realmHistory.forEach(r => {
      items.push({
        type: 'realm',
        realm: r.realm,
        realmIndex: realmOrder.indexOf(r.realm),
        cosmicScale: r.cosmicScale,
        description: r.event,
        chapter: r.chapter,
        episode: r.episode,
      });
    });

    data.fortunes.forEach(f => {
      items.push({
        type: 'fortune',
        title: f.name,
        realm: f.realm,
        realmIndex: realmOrder.indexOf(f.realm),
        description: f.description,
        significance: f.significance,
      });
    });

    data.events.forEach(ev => {
      items.push({
        type: 'event',
        title: ev.name,
        realm: ev.realm,
        realmIndex: realmOrder.indexOf(ev.realm),
        description: ev.description,
        significance: ev.significance,
      });
    });

    // 按境界索引降序（从低到高 → 时间线从上到下是从凡人到高阶）
    items.sort((a, b) => (a.realmIndex - b.realmIndex));

    return items;
  }

  function renderTimelineNode(item, charColor) {
    if (item.type === 'realm') {
      const scale = getCosmicScale(item.cosmicScale);
      return `
        <div class="timeline-node realm-breakthrough"
             style="--node-accent: ${scale.accent}; --node-glow: ${scale.accent}44; --node-gradient: ${scale.gradient};">
          <div class="node-header">
            <span class="node-realm" style="color: ${scale.accent};">${scale.icon} ${item.realm}</span>
            <span class="node-badge" style="background: ${scale.accent};">境界突破</span>
          </div>
          <p class="node-description">${item.description}</p>
          <div class="node-meta">
            <span>📖 ${item.chapter}</span>
            <span>📺 ${item.episode}</span>
            <span>🌐 ${scale.label}</span>
          </div>
        </div>
      `;
    }

    if (item.type === 'fortune' || item.type === 'event') {
      const tagClass = item.type === 'fortune' ? 'fortune' : 'event';
      const tagLabel = item.type === 'fortune' ? '机缘' : '事件';
      const accentColor = item.type === 'fortune' ? 'var(--gold)' : '#8ac';
      return `
        <div class="timeline-node event-node ${tagClass}"
             style="--node-accent: ${accentColor};">
          <span class="event-tag ${tagClass}">${tagLabel}</span>
          <div class="node-header">
            <span class="node-realm" style="color: ${charColor};">${item.title}</span>
            ${item.significance ? `<span class="node-badge" style="background: ${charColor};">${item.significance}</span>` : ''}
          </div>
          <p class="node-description">${item.description}</p>
          <div class="node-meta">
            <span>📌 ${item.realm}时期</span>
          </div>
        </div>
      `;
    }

    return '';
  }

  function renderRelationships(data) {
    const container = document.getElementById('relationships-container');
    if (!container || !data.relationships.length) return;

    container.innerHTML = `
      <section class="timeline-section">
        <h2 class="timeline-title">人物关系</h2>
        <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:12px;">
          ${data.relationships.map(r => `
            <div style="background:var(--bg-card);border:1px solid var(--border-subtle);border-radius:var(--radius-sm);padding:14px 16px;">
              <div style="font-weight:600;color:${data.color};margin-bottom:4px;">${r.name}</div>
              <div style="font-size:0.8rem;color:var(--gold);">${r.relation}</div>
              <div style="font-size:0.8rem;color:var(--text-dim);margin-top:4px;">${r.note}</div>
            </div>
          `).join('')}
        </div>
      </section>
    `;
  }

  function renderQuotes(data) {
    const container = document.getElementById('quotes-container');
    if (!container || !data.quotes.length) return;

    container.innerHTML = `
      <section class="timeline-section">
        <h2 class="timeline-title">经典台词</h2>
        ${data.quotes.map(q => `
          <blockquote style="
            margin-bottom:16px;
            padding:16px 20px;
            background:var(--bg-card);
            border-left:3px solid ${data.color};
            border-radius:0 var(--radius-sm) var(--radius-sm) 0;
            font-family:var(--font-display);
            font-size:1.05rem;
            font-style:italic;
            color:var(--text-primary);
          ">
            "${q.text}"
            <footer style="margin-top:8px;font-size:0.8rem;font-style:normal;color:var(--text-dim);">${q.context}</footer>
          </blockquote>
        `).join('')}
      </section>
    `;
  }
})();
```

- [ ] **Step 3: 验证 — 打开 character.html?id=hanli**

Expected:
1. "修仙之路"时间线从上到下展示韩立的境界突破（凡人地壳→地幔→地核→练气→筑基→结丹→元婴→大乘）
2. 每个境界节点有对应宇宙尺度的颜色和背景
3. 机缘（金色标签）和事件（蓝色标签）穿插在时间线上
4. 人物关系网格展示南宫婉、紫灵等
5. 经典台词以引用块展示

- [ ] **Step 4: 提交**

```bash
git add css/timeline.css js/timeline.js
git commit -m "feat: add realm timeline, relationships, and quotes sections"
```

---

### Task 8: 首页导航增强 + 角色列表

**Files:**
- Modify: `index.html` — 在储物袋下方添加角色快捷列表
- Modify: `js/storage-bag.js` — 无需修改，已有筛选功能

- [ ] **Step 1: 在 storage-bag.css 末尾追加角色快捷导航样式**

追加到 `D:\桌面凡人回忆录\css\storage-bag.css`：

```css
/* === 角色快捷导航（储物袋下方） === */
.quick-nav {
  text-align: center;
  margin-top: 20px;
  opacity: 0;
  transition: opacity 0.5s 0.5s;
}
.quick-nav.visible { opacity: 1; }
.quick-nav-title {
  font-family: var(--font-display);
  font-size: 0.9rem;
  color: var(--text-dim);
  margin-bottom: 12px;
}
.quick-nav-links {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 10px;
}
.quick-nav-link {
  display: inline-block;
  padding: 8px 20px;
  background: var(--bg-card);
  border: 1px solid var(--border-subtle);
  border-radius: 20px;
  color: var(--text-secondary);
  text-decoration: none;
  font-size: 0.9rem;
  transition: all 0.2s;
}
.quick-nav-link:hover {
  border-color: var(--border-gold);
  color: var(--gold-light);
  box-shadow: 0 0 12px var(--gold-glow);
}
```

- [ ] **Step 2: 在 index.html 的 storage-page 末尾添加快捷导航 HTML**

在 `index.html` 的 `</div><!-- .storage-page -->` 前添加：

```html
    <div class="quick-nav" id="quick-nav">
      <p class="quick-nav-title">— 或直接浏览 —</p>
      <div class="quick-nav-links" id="quick-nav-links"></div>
    </div>
```

- [ ] **Step 3: 在 storage-bag.js 的 openBag 函数中添加快捷导航渲染**

在 `storage-bag.js` 的 `openBag()` 函数中，`filterBar.classList.add('visible');` 之后添加：

```javascript
    // 渲染快捷导航
    const quickNav = document.getElementById('quick-nav');
    const quickNavLinks = document.getElementById('quick-nav-links');
    quickNavLinks.innerHTML = characterList.map(c =>
      `<a href="character.html?id=${c.id}" class="quick-nav-link">${c.name}</a>`
    ).join('');
    quickNav.classList.add('visible');
```

在 `closeBag()` 函数中添加 `document.getElementById('quick-nav').classList.remove('visible');`

- [ ] **Step 4: 验证**

Expected: 点击储物袋后，法宝下方出现"或直接浏览"文字链接列表，可直接跳转角色页。

- [ ] **Step 5: 提交**

```bash
git add css/storage-bag.css index.html js/storage-bag.js
git commit -m "feat: add quick navigation links below storage bag"
```

---

### Task 9: 响应式适配 + 细节打磨

**Files:**
- Modify: `css/style.css`
- Modify: `css/storage-bag.css`
- Modify: `css/character-card.css`
- Modify: `css/timeline.css`

- [ ] **Step 1: 添加移动端响应式样式**

在 `style.css` 末尾追加：

```css
/* === 响应式 === */
@media (max-width: 768px) {
  .site-title { font-size: 2rem; }
  .site-subtitle { font-size: 0.9rem; letter-spacing: 0.15em; }
  .breadcrumb { padding: 14px 16px; }
}
```

在 `storage-bag.css` 末尾追加：

```css
@media (max-width: 768px) {
  .treasures-area { gap: 16px; padding: 20px 10px; }
  .treasure-card { width: 80px; height: 110px; }
  .treasure-icon { font-size: 1.5rem; }
  .treasure-name { font-size: 0.8rem; }
}
```

在 `character-card.css` 末尾追加：

```css
@media (max-width: 768px) {
  .character-name { font-size: 1.8rem; }
  .stat-panel-header { padding: 20px 20px 14px; }
  .cosmic-indicator { margin: 0 20px 14px; }
  .stat-grid { grid-template-columns: 1fr 1fr; }
  .treasure-grid { grid-template-columns: 1fr 1fr; padding: 14px 20px; }
  .story-section { padding: 20px; }
}
```

在 `timeline.css` 末尾追加：

```css
@media (max-width: 768px) {
  .timeline { padding-left: 30px; }
  .timeline::before { left: 13px; }
  .timeline-node::before { left: -23px; width: 10px; height: 10px; }
  .timeline-node.realm-breakthrough::before { width: 16px; height: 16px; left: -26px; }
}
```

- [ ] **Step 2: 添加页面过渡动画**

在 `style.css` 末尾追加：

```css
/* === 页面淡入 === */
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
.character-page {
  animation: fadeInUp 0.5s ease-out;
}
.storage-page {
  animation: fadeInUp 0.5s ease-out;
}
```

- [ ] **Step 3: 浏览器中等比例缩放测试**

在 Chrome 中：
1. 打开 `index.html`，按 F12 → 切换设备工具栏（Ctrl+Shift+M）
2. 选择 iPhone 12 Pro (390×844)
3. 验证储物袋不超出屏幕、法宝卡片正常排列
4. 打开 `character.html?id=hanli`，验证面板和布局正常

- [ ] **Step 4: 提交**

```bash
git add css/style.css css/storage-bag.css css/character-card.css css/timeline.css
git commit -m "style: add responsive breakpoints and page transitions"
```

---

### Task 10: README + 最终验证

**Files:**
- Create: `README.md`

- [ ] **Step 1: 创建 README**

写入 `D:\桌面凡人回忆录\README.md`：

```md
# 凡人修仙传 · 角色回忆录

一个记录动漫《凡人修仙传》角色生平的粉丝向回忆录/百科网站。

## 技术栈

- HTML5 + CSS3 + Vanilla JavaScript
- 纯静态网站，无需构建工具
- 数据存储在 JSON + Markdown 文件中

## 本地运行

直接用浏览器打开 `index.html` 即可。或使用任意静态服务器：

```bash
# Python 3
python -m http.server 8080

# Node.js (npx)
npx serve .
```

然后访问 `http://localhost:8080`

## 项目结构

```
├── index.html          # 首页（储物袋入口）
├── character.html      # 角色详情页
├── css/                # 样式文件
├── js/                 # 脚本文件
├── data/characters/    # 角色数据（JSON + MD）
└── assets/             # 图片、字体、图标
```

## 添加新角色

1. 复制 `data/characters/_template/` 到新文件夹
2. 编辑 `data.json` 填写角色信息
3. 编辑 `story.md` 填写角色传记
4. 在 `data/characters/index.json` 中添加条目

## 设计理念

- **储物袋交互**：点击储物袋 → 角色法宝飞出，贴合修仙世界观
- **游戏化时间线**：RPG 属性面板 + 纵向境界时间轴
- **境界 → 宇宙尺度映射**：凡人（地球三层）→ 练气（行星）→ 筑基（恒星系统）→ 结丹（星系）→ 元婴（本星系群）
```

- [ ] **Step 2: 端到端验证**

```bash
# 列出所有已创建的文件
find "D:\桌面凡人回忆录" -type f -not -path "*/.git/*" | sort
```

确认关键文件全部存在：
- index.html ✓
- character.html ✓
- css/style.css ✓
- css/storage-bag.css ✓
- css/character-card.css ✓
- css/timeline.css ✓
- js/data-loader.js ✓
- js/cosmic-scale.js ✓
- js/storage-bag.js ✓
- js/character-page.js ✓
- js/timeline.js ✓
- data/characters/index.json ✓
- data/characters/hanli/data.json ✓
- data/characters/hanli/story.md ✓
- data/characters/_template/data.json ✓
- data/characters/_template/story.md ✓

- [ ] **Step 3: 浏览器完整流程测试**

1. 打开 `index.html` → 看到深色背景 + 金色标题 + 浮动储物袋
2. 点击储物袋 → 袋子打开，韩立法宝飞出（绿色光晕）
3. 筛选栏和快捷导航出现
4. 点击韩立法宝（或快捷链接）→ 跳转 `character.html?id=hanli`
5. 角色详情页展示完整：属性面板（绿色主题）、宇宙视野、时间线、关系、台词
6. 点击"返回首页"→ 回到储物袋页面

- [ ] **Step 4: 最终提交**

```bash
git add README.md
git add -A
git commit -m "docs: add README and finalize Phase 1 implementation"
```

---

## 实现总结

| 阶段 | 任务 | 产出 |
|---|---|---|
| A. 基础 | 1-3 | 页面骨架、CSS 主题、数据模型、加载模块 |
| B. 首页 | 4-5 | 储物袋 CSS 绘制 + 点击弹出法宝交互 |
| C. 角色页 | 6-7 | 属性面板 + 宇宙视野 + 时间线 + 关系 + 台词 |
| D. 收尾 | 8-10 | 导航增强、响应式、README、端到端验证 |

**总计 10 个任务，预计 45-60 分钟完成全部实现。**
